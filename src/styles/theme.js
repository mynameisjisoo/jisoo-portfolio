const colors = {
  black: '#222831',
  white: '',
  mint: '#82CBC4',
  silver: '#EEEEEE'
};

const fonts = {
  family: {},
  size: {
    base: '1.5rem'
  }
};

const darkThemeColors = {
  ...colors
};

const defaultTheme = {};

const theme = {
  colors,
  fonts
};

export default theme;
